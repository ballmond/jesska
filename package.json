{
  "private": true,
  "name": "jesska",
  "version": "0.0.1",
  "scripts": {
    "build": "lerna run build --parallel",
    "build-web": "lerna bootstrap && (cd packages/gbcbb && npm run export)",
    "develop": "lerna run dev --parallel",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-json": "^4.1.0",
    "dotenv": "^8.2.0",
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "marked": "^1.2.7"
  }
}
